# Python 3.10+ recommended
requests>=2.31          # HTTP client untuk es_fetch.py (koneksi ke Elasticsearch)
pandas>=2.0
numpy>=1.24,<3
scikit-learn>=1.3
sentence-transformers>=2.7
bertopic>=0.16.3
umap-learn>=0.5.5
hdbscan>=0.8.33
pyarrow>=14.0          # parquet support
plotly>=5.18           # BERTopic visualizations
networkx>=3.0          # visualisasi graph topik (topic_graph.html)
safetensors>=0.4
rake-nltk>=1.0.6       # ekstraksi keyphrase RAKE (kolom RakeKeywords di topics_summary.csv)
langdetect>=1.0.9      # deteksi bahasa (fallback Python murni, selalu terpasang)
igraph>=0.11           # graf untuk Leiden community detection
leidenalg>=0.10        # Leiden algorithm (meta-topic grouping)
# Backend deteksi bahasa yang disarankan untuk jutaan baris (jauh lebih cepat
# daripada langdetect; roda prabangun, tanpa kompilator C). Opsional:
#   pip install fasttext-wheel

# Server API (api.py) — jalankan pipeline dari jarak jauh lewat HTTP/SSH/Termius
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # unggah berkas (Form/File) di FastAPI
