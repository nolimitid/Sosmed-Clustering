# Large data dumps — exceed GitHub's 100 MB limit; keep these locally only
report_raw_posts_*.json

# Pipeline outputs (embedding cache/memmap can be several GB)
results/

# API job dirs (uploaded inputs, per-job outputs, logs)
api_jobs/

# Virtual environment — jangan pernah di-commit
venv/
.venv/
env/

# Data pipeline output (bisa puluhan GB)
data/
results*/

# Python bytecode
__pycache__/
*.pyc
*.pyo

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
